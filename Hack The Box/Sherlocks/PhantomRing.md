## Analysis
- First I check its Sha-256 encoded with it.
- Then I get the IP of the C2 server(attackers server/system) using strings command.
- Using the IP I tried it on browser but it did not show any results, so I ran the agent in my terminal though it just said failed to connect.
-  Then I used netstat on it and it returned me many server addresses.
